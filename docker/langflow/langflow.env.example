# OpenAI-compatible provider settings for Langflow components.
# If you use Mistral via OpenAI-compatible API, set these values.
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Optional provider keys that may be used directly in flows.
MISTRAL_API_KEY=
